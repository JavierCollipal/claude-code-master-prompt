# MongoDB Atlas Connection (REQUIRED)
# Get your connection string from MongoDB Atlas dashboard
# Format: mongodb+srv://[USERNAME]:[PASSWORD]@[CLUSTER].mongodb.net/[DATABASE]?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://[USERNAME]:[PASSWORD]@[CLUSTER].mongodb.net/[DATABASE]?retryWrites=true&w=majority

# Database Names for Each Personality (REQUIRED)
NEKO_DATABASE=neko-defense-system
MARIO_DATABASE=marionnette-theater
NOEL_DATABASE=noel-precision-archives
GLAM_DATABASE=glam-street-chronicles
HANNIBAL_DATABASE=hannibal-forensic-archives
TETORA_DATABASE=tetora-mpd-archives

# Cypress Cloud Configuration (OPTIONAL)
# Get from https://cloud.cypress.io
CYPRESS_PROJECT_ID=[YOUR_PROJECT_ID]
CYPRESS_RECORD_KEY=[YOUR_RECORD_KEY]

# GitHub Token (OPTIONAL)
# For automated operations, get from https://github.com/settings/tokens
GITHUB_TOKEN=[YOUR_GITHUB_TOKEN]

# Environment Configuration
NODE_ENV=development

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file with real credentials
# 2. Copy this file to .env and fill in your actual values
# 3. The .env file is already in .gitignore for your protection
# 4. Rotate credentials every 30-90 days for security